#include <stdio.h>

void fReadLine(char* line, FILE* fp);

int main(int argc, char* argv[])
{
  FILE* fp;
  char* mode;
  
  if (argc == 0)
  {
  }
  
  fp = fopen(argv[], mode);
  return 0;
}

void fReadLine(char* line, FILE* fp)
{
}
